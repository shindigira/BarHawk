var uberController = require('./uberController.js');

module.exports = function (app) {
  //app.get('/ubergetestimates', customerController.getUberInfoFn);
  //app.get('/getuberinfo', uberController.getUberInfo);
};
